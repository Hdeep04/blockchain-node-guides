# 第4部・応用編 第3章：マイノリティクライアントへの移行
## ～ Nethermind + Nimbus 構築記録 ～

> **第1部・第2部で構築したGeth + Lighthouseから、マイノリティクライアントであるNethermind + Nimbusへの移行を検証した記録**

---

| 項目 | 内容 |
|---|---|
| 位置づけ | 第4部・応用編の第3章。マイノリティクライアントの構築と第1部・第2部との比較 |
| 検証環境 | VirtualBox VM（Ubuntu 22.04 LTS）× 1台（testnetcsm） |
| 前提 | 第1部・第2部でGeth + Lighthouseの構築が完了していること |
| 検証日 | 2026年6月13日（Hoodi Testnet） |

> ⚠️ **本書は機密情報を一切含みません。** アドレス類は `<...>` のプレースホルダです。

> 💡 **リンクは右クリック→「新しいタブで開く」を選択すると
> 手順書を表示したまま参照先を確認できます。**

---

## 1. マイノリティクライアントとは

Ethereumネットワークには複数の実装（クライアント）が存在します。ノードオペレーターがどのクライアントを使うかは自由ですが、特定のクライアントに集中しすぎると**ネットワーク全体のリスクが高まります。**

### クライアント多様性の重要性

````
【危険なシナリオ】

あるクライアントが全ノードの66%以上を占めている場合：
  そのクライアントに重大なバグが発生
      ↓
  66%以上のノードが誤った判断をする
      ↓
  チェーンが不正なまま確定（ファイナライズ）
      ↓
  大規模なスラッシング発生
  ネットワーク停止の可能性
````

> 💡 **33%ルール：**
> Ethereum.orgは「どのクライアントも33%を超えるべきではない」と推奨しています。
> 33%を超えると単一障害点になりうるからです。

### 2026年現在のクライアントシェア

**実行レイヤー（EL）：**

| クライアント | シェア | 評価 |
|---|---|---|
| **Geth** | **41%** | 多すぎる・削減すべき |
| **Nethermind** | **38%** | 適切 |
| Besu | 16% | 少ない |
| Reth | 5.4% | 少ない |

**合意レイヤー（CL）：**

| クライアント | シェア | 評価 |
|---|---|---|
| **Lighthouse** | **52.65%** | 多すぎる・削減すべき |
| Prysm | 17.66% | やや多い |
| Teku | 13.36% | 適切 |
| **Nimbus** | **9.7%** | 少ない・使うべき |

### なぜNethermind + Nimbusを選ぶのか

````
Geth（41%）の代わりにNethermindを使う
  → 実行レイヤーの多様化に貢献

Lighthouse（52%）の代わりにNimbusを使う
  → 合意レイヤーの多様化に貢献

さらに：
  Nimbusは「軽量・省リソース」が特徴
  → ホームステーキングに最適
  → 低スペックPCでも運用可能
````

---

## 2. 第1部・第2部との比較

### サービス・ユーザー・データ場所の対比

| 項目 | Geth（第1部・第2部） | Nethermind（本章） |
|---|---|---|
| サービス名 | `geth` | `nethermind` |
| 実行ユーザー | `ethereum` | `nethermind` |
| データ場所 | `/var/lib/ethereum/geth` | `/var/lib/nethermind` |
| JWT場所 | `/var/lib/ethereum/jwt/jwt.hex` | `/secrets/jwtsecret` |
| RPC Port | `8545` | `8545` |
| Engine Port | `8551` | `8551` |
| P2P Port | `30303` | `30303` |
| ネットワーク指定 | `--hoodi` | `--config hoodi` |
| Pruning | なし | `Hybrid`（標準搭載） |

| 項目 | Lighthouse（第1部・第2部） | Nimbus（本章） |
|---|---|---|
| サービス名 | `lighthouse` | `nimbus` |
| 実行ユーザー | `ethereum` | `consensus` |
| データ場所 | `/var/lib/ethereum/lighthouse` | `/var/lib/nimbus` |
| JWT場所 | `/var/lib/ethereum/jwt/jwt.hex` | `/secrets/jwtsecret` |
| REST Port | `5052` | `5052` |
| Metrics Port | `5054` | `8008` |
| fee-recipient設定場所 | VC側 | BN側・VC側どちらでも可（VC側が優先） |
| Doppelgänger Detection | 無効（デフォルト） | **有効（デフォルト）** ✅ |

### コマンドの違い

**Geth vs Nethermind：ネットワーク指定**

```bash
# Geth：フラグで指定
ExecStart=/usr/bin/geth \
  --hoodi \          ← フラグ

# Nethermind：設定ファイル名で指定
ExecStart=/usr/local/bin/nethermind/nethermind \
  --config hoodi \   ← 設定ファイル名
```

**Lighthouse vs Nimbus：同期方法**

```bash
# Lighthouse：サービス起動時にCheckpoint Sync
ExecStart=/usr/local/bin/lighthouse beacon_node \
  --checkpoint-sync-url https://...   ← 起動オプション

# Nimbus：起動前に別コマンドで実行
sudo -u consensus /usr/local/bin/nimbus_beacon_node trustedNodeSync \
  --network=hoodi \
  --trusted-node-url=https://hoodi.beaconstate.ethstaker.cc \
  --data-dir=/var/lib/nimbus \
  --backfill=false
```

> 💡 **NimbusのCheckpoint Syncは驚異的な速さでした。**
> 今回の実測：**わずか約2分15秒で完了**
> （03:13:47 開始 → 03:16:02 完了）

---

## 3. 検証環境

### testnetcsmの状態（構築前）

````
サービス：geth / lighthouse / lighthouse-vc 全て inactive・disabled
削除したデータ：
  /var/lib/ethereum/geth/      ← Gethのチェーンデータ
  /var/lib/ethereum/lighthouse/ ← Lighthouseのビーコンデータ
  /var/lib/lido-csm/validators/（古い鍵） ← スラッシング済み鍵

残したデータ：
  /var/lib/lido-csm/           ← api-token・logsのみ（鍵は削除済み）
  /etc/systemd/system/         ← 既存サービスファイル（geth・lighthouse）
                                  新規でnimbus・nethermind・nimbus-vcを追加
````

### 新規作成したリソース

````
/secrets/jwtsecret             ← JWT Secret（新規生成）
/var/lib/nethermind/           ← Nethermindデータディレクトリ
/var/lib/nimbus/               ← Nimbusデータディレクトリ（VCが使用）
/var/lib/nimbus-bn/            ← BN用空ディレクトリ（BNのkeystore競合回避）
````

---

## 4. Nethermind のインストール（実行レイヤー・EL）

### ユーザーとディレクトリの作成

```bash
# Nethermind専用ユーザーの作成
# なぜ専用ユーザーを作るのか：
# 万が一の侵害時にシステム全体への影響を最小化するため
sudo useradd --no-create-home --shell /bin/false nethermind
sudo mkdir -p /var/lib/nethermind
sudo chown -R nethermind:nethermind /var/lib/nethermind
```

### バイナリのダウンロードとインストール

```bash
# 最新バージョンを自動取得
RELEASE_URL="https://api.github.com/repos/NethermindEth/nethermind/releases/latest"
BINARIES_URL="$(curl -s $RELEASE_URL | jq -r '.assets[] | select(.name) | .browser_download_url' | grep linux-x64 | grep -v asc)"

echo "Downloading URL: $BINARIES_URL"

cd ~
wget -O nethermind.zip $BINARIES_URL
sudo mkdir -p /usr/local/bin/nethermind
sudo unzip nethermind.zip -d /usr/local/bin/nethermind
rm nethermind.zip

# バージョン確認
/usr/local/bin/nethermind/nethermind --version
```

**今回の実測：**
````
Version: 1.38.0+c07a4d65
Runtime: .NET 10.0.7
Platform: Linux x64
````

### JWT Secretの作成

```bash
# /secrets フォルダを作成
# なぜ/secrets か：
# システム全体で使う認証情報を一か所にまとめるため
sudo mkdir -p /secrets
openssl rand -hex 32 | tr -d "\n" | sudo tee /secrets/jwtsecret
sudo chmod 644 /secrets/jwtsecret
```

### systemdサービスの設定

```bash
sudo vi /etc/systemd/system/nethermind.service
```

```ini
[Unit]
Description=Nethermind Execution Layer Client service for Hoodi
Wants=network-online.target
After=network-online.target
Documentation=https://www.coincashew.com

[Service]
Type=simple
User=nethermind
Group=nethermind
Restart=always
RestartSec=3
KillSignal=SIGINT
TimeoutStopSec=900
WorkingDirectory=/var/lib/nethermind
Environment="DOTNET_BUNDLE_EXTRACT_BASE_DIR=/var/lib/nethermind"
ExecStart=/usr/local/bin/nethermind/nethermind \
  --config hoodi \
  --datadir="/var/lib/nethermind" \
  --Network.DiscoveryPort 30303 \
  --Network.P2PPort 30303 \
  --Network.MaxActivePeers 50 \
  --JsonRpc.Port 8545 \
  --JsonRpc.EnginePort 8551 \
  --Metrics.Enabled true \
  --Metrics.ExposePort 6060 \
  --JsonRpc.JwtSecretFile /secrets/jwtsecret

[Install]
WantedBy=multi-user.target
```

### 起動と確認

```bash
sudo systemctl daemon-reload
sudo systemctl enable nethermind
sudo systemctl start nethermind
sudo systemctl is-active nethermind
```

```bash
# ログをリアルタイムで確認する
# 「Initialization Completed」が表示されれば起動成功
sudo journalctl -fu nethermind
```

**起動直後の重要なログ：**

````
Initialization Completed
Chain ID: Hoodi
JSON RPC: http://127.0.0.1:8545 ; http://127.0.0.1:8551
Reading authentication secret from '/secrets/jwtsecret'
Waiting for Forkchoice message from Consensus Layer
````

> 💡 **「Waiting for Forkchoice message from Consensus Layer」について：**
> Post-Merge以降のEthereumでは、EL（Nethermind）はCLからの
> Forkchoiceメッセージが届くまで同期を開始できません。
> これはGethも同様の動作です。
> 第1部・第2部でもGeth起動後にLighthouseを起動するまで
> 同じ状態が続いていました。
> CLクライアント（Nimbus）を起動することで同期が始まります。
> これは正常な動作です。

---

## 5. Nimbus のインストール（合意レイヤー・CL / Beacon Node）

### ユーザーとディレクトリの作成

```bash
# consensus専用ユーザーの作成
sudo adduser --system --no-create-home --group consensus
sudo mkdir -p /var/lib/nimbus
sudo chown -R consensus:consensus /var/lib/nimbus

# BN用の空ディレクトリを作成（重要）
# なぜ別ディレクトリが必要か：
# Nimbus BNは --in-process-validators=false を指定しても
# 同じ --data-dir のvalidatorsディレクトリのkeystoreをロックしてしまう
# BNとVCが同じvalidatorsディレクトリを共有するとVCが起動できなくなる
sudo mkdir -p /var/lib/nimbus-bn/validators
sudo mkdir -p /var/lib/nimbus-bn/secrets
sudo chown -R consensus:consensus /var/lib/nimbus-bn

# 依存パッケージのインストール
sudo apt install libsnappy-dev libc6-dev libc6 ccze -y
```

> ⚠️ **`/var/lib/nimbus-bn/` は必ず作成してください。**
> この手順を省略するとNimbus VCが起動できなくなります。
> また `/var/lib/nimbus-bn/` のパーミッションは
> `consensus` ユーザーが所有者であれば動作します。
> 詳細はセクション7のトラブルシューティングを参照してください。

### バイナリのダウンロードとインストール

```bash
RELEASE_URL="https://api.github.com/repos/status-im/nimbus-eth2/releases/latest"
BINARIES_URL="$(curl -s $RELEASE_URL | jq -r '.assets[] | select(.name) | .browser_download_url' | grep _Linux_amd64 | grep -v asc)"

echo "Downloading URL: $BINARIES_URL"

cd ~
wget -O nimbus.tar.gz $BINARIES_URL
tar -xzvf nimbus.tar.gz -C ~
mv ~/nimbus-eth2_Linux_amd64_* ~/nimbus
sudo mv ~/nimbus/build/nimbus_beacon_node /usr/local/bin
sudo mv ~/nimbus/build/nimbus_validator_client /usr/local/bin
rm nimbus.tar.gz
rm -rf ~/nimbus

# バージョン確認
nimbus_beacon_node --version
```

**今回の実測：**
````
Nimbus beacon node v26.5.0-6fb05f-stateofus
Ethereum consensus spec v1.7.0-alpha.7
````

### Checkpoint Syncで高速同期

```bash
# 起動前に信頼できるノードから最新状態を取得する
# なぜCheckpoint Syncを使うのか：
# 通常の同期は数時間〜数日かかるが
# Checkpoint Syncを使えば数分で完了する
sudo -u consensus /usr/local/bin/nimbus_beacon_node trustedNodeSync \
  --network=hoodi \
  --trusted-node-url=https://hoodi.beaconstate.ethstaker.cc \
  --data-dir=/var/lib/nimbus \
  --backfill=false
```

**完了メッセージ：**
````
NTC Done, your beacon node is ready to serve you!
````

> 💡 **Checkpoint Syncの実測時間：約2分15秒**
> 03:13:47 開始 → 03:16:02 完了
> Lighthouseの場合と同様に数分で完了します。

### systemdサービスの設定

```bash
sudo vi /etc/systemd/system/nimbus.service
```

```ini
[Unit]
Description=Nimbus Consensus Layer Client service for Hoodi
Wants=network-online.target
After=network-online.target
Documentation=https://www.coincashew.com

[Service]
Type=simple
User=consensus
Group=consensus
Restart=on-failure
RestartSec=3
KillSignal=SIGINT
TimeoutStopSec=900
ExecStart=/usr/local/bin/nimbus_beacon_node \
  --network=hoodi \
  --data-dir=/var/lib/nimbus \
  --validators-dir=/var/lib/nimbus-bn/validators \
  --secrets-dir=/var/lib/nimbus-bn/secrets \
  --tcp-port=9000 \
  --udp-port=9000 \
  --max-peers=100 \
  --rest-port=5052 \
  --enr-auto-update=true \
  --non-interactive \
  --status-bar=false \
  --in-process-validators=false \
  --web3-url=http://127.0.0.1:8551 \
  --rest \
  --metrics \
  --metrics-port=8008 \
  --jwt-secret="/secrets/jwtsecret" \
  --suggested-fee-recipient=<Lido_CSM_Fee_Recipient_Address>

[Install]
WantedBy=multi-user.target
```

> 💡 **`--validators-dir` と `--secrets-dir` について：**
> BN用の空ディレクトリを指定します。
> これによりBNはVC用のkeystoreをロックしなくなります。
> `--in-process-validators=false` だけでは不十分で
> この設定が必須です。

> 💡 **`--suggested-fee-recipient` について：**
> Nimbusでは**BN側**でfee-recipientを設定します。
> Lighthouseでは**VC側**で設定していた点が異なります。
>
> | ネットワーク | Lido CSM Fee Recipient Address |
> |---|---|
> | Hoodi Testnet | `0x9b108015fe433F173696Af3Aa0CF7CDb3E104258` |
> | Mainnet | `0x388C818CA8B9251b393131C08a736A67ccB19297` |

> 💡 **`--in-process-validators=false` について：**
> BNとVCを分離して動かすための設定です。
> Nimbusは本来BNとVCを一体化して動かすこともできますが
> 第1部・第2部と同様にBN/VC分離構成を採用します。

> 💡 **`doppelgangerDetection: true`（デフォルト有効）**
> Nimbusは**デフォルトでDoppelgänger Detectionが有効**です。
> 第2章で検証したtestnetcsmでは無効になっていましたが
> Nimbusでは何もしなくても有効になっています。

### 起動と確認

```bash
sudo systemctl daemon-reload
sudo systemctl enable nimbus
sudo systemctl start nimbus
sudo systemctl is-active nimbus
```

### 同期状態の確認

**Nimbus BN（合意レイヤー）の同期確認：**

```bash
# 同期状態をAPIで確認する
curl -s http://127.0.0.1:5052/eth/v1/node/syncing | jq
```

| 項目 | 正常値 | 意味 |
|---|---|---|
| `is_syncing` | `false` | 同期完了 ✅ |
| `is_optimistic` | `false` | ELの検証完了 ✅ |
| `sync_distance` | `0` | 最新ブロックに追いついた ✅ |

> 💡 **`is_optimistic: true` について：**
> Nimbusの同期は完了しているが
> Nethermindがブロック検証中の過渡状態です。
> Nethermindの同期完了後に `false` になります。

```bash
# ログでbackfillの進捗を確認する
# sync="synced" が表示されれば完全同期完了
sudo journalctl -u nimbus --no-pager | grep "Slot start" | tail -3
```

**Nethermind（実行レイヤー）の同期確認：**

```bash
# Nethermindの同期状態を確認する
curl -s http://127.0.0.1:8545 \
  -X POST \
  -H "Content-Type: application/json" \
  --data '{"jsonrpc":"2.0","method":"eth_syncing","params":[],"id":1}' | jq
```

| 結果 | 意味 |
|---|---|
| `"result": false` | 同期完了 ✅ |
| `currentBlock` と `highestBlock` が同値 | Old Bodies補完中（バリデータ動作可能） |
| `currentBlock: 0x0` | State同期中 |

> 💡 **2つのクライアントの同期は独立しています。**
> NimbusとNethermindはそれぞれ別々に同期します。
> どちらが先に完了しても問題ありません。
> 両方が同期完了して初めて完全な動作状態になります。

```bash
# ログをリアルタイムで確認する
# 「Slot start」が表示されれば起動成功
sudo journalctl -fu nimbus
```

**正常起動時のログ例：**
````
NTC Starting REST HTTP server  url=http://127.0.0.1:5052
NTC Starting beacon node       validators=0
INF Slot start  slot=XXXXXX  epoch=XXXXXX  peers=X
````

> 💡 **Nethermindのログを別ターミナルで確認する**
> Nimbusを起動した後、Nethermindが同期を開始したか
> 別のターミナルを開いて以下のコマンドで確認できます：
> ```bash
> sudo journalctl -u nethermind --no-pager | tail -10
> ```
> 以下のようなログが出ればNimbusからのForkchoiceを受信して
> 同期が始まっています：
> ```
> Received ForkChoice: ...
> New beacon pivot: ...
> Old Headers X / X (XX%)
> ```

**Nimbusが起動するとNethermindに変化が現れます：**
````
Received ForkChoice: ...
New beacon pivot: ...
Old Headers X / X (XX%)
Snap State Ranges (Phase 1): X%
````

### 同期時リソース実測値（参考）

> 参考実測値（Hoodi Testnet / VM環境 / Ubuntu 22.04 / RAM 15GB / SSD 292GB）

**Nethermind同期4段階のリソース推移：**

| 段階 | 内容 | 所要時間 | CPU |
|---|---|---|---|
| Old Headers | ブロックヘッダー取得 | 約21分 | 中 |
| Snap State Phase 1 | State同期 | 約1時間10分 | 高 |
| Large storage | DeFiコントラクト | 約13分 | 高 |
| Old Bodies | 過去ブロック補完 | 約1時間31分 | 中 |
| **合計** | **完全同期** | **約3時間15分** | - |

**Nimbus Checkpoint Sync：**

| 項目 | 実測値 |
|---|---|
| Checkpoint Sync完了 | **約2分15秒** |
| backfill完了 | 約8時間32分（バックグラウンド） |
| 署名開始まで | **Checkpoint Sync完了後すぐ** |

**同期中ピーク時のリソース：**

| 項目 | 値 |
|---|---|
| メモリ使用 | 5.6GB |
| Swap | 954MB |
| Nethermind CPU | 223%（ピーク） |
| Nimbus BN CPU | 56.5% |

---

## 6. Nimbus VC のインストール（バリデータクライアント・VC）

> ⚠️ **【重要】Nimbus BN/VC分離構成について**
>
> 本章ではLighthouseと同様にBN/VC分離構成を採用していますが
> **Nimbusの公式ドキュメントはこの構成を上級者向けと位置づけています。**
>
> Nimbus公式の警告：
> 「これまでに確認されたスラッシングのほとんどは
> BNとVCを分離した複雑なセットアップに起因しています。
> リスクを軽減する手順を踏んだ場合にのみ使用してください。」
>
> 📎 [Nimbus公式ドキュメント：Run a separate validator client](https://nimbus.guide/validator-client.html)
>
> **Nimbusの推奨構成はBN+VC統合（in-process validator）です。**
> 統合構成では1つのサービスでBNとVCを同時に動かすため
> keystoreの競合が起きず、よりシンプルで安全です。
>
> 本章ではあえて分離構成を採用し、発生したトラブルも含めて
> 記録することで読者の学びとしています。

### Lighthouseとのコマンド比較

| 項目 | Lighthouse VC | Nimbus VC |
|---|---|---|
| インポートコマンド | `lighthouse account validator import` | `nimbus_beacon_node deposits import` |
| 実行ユーザー | `ethereum` | `consensus` |
| データdir | `/var/lib/lido-csm` | `/var/lib/nimbus` |
| 結果メッセージ | `Successfully imported 1 validators` | `Keystore imported` |
| サービス名 | `lighthouse-vc` | `nimbus-vc` |
| BN接続オプション | `--beacon-nodes` | `--beacon-node` |
| Metrics Port | `8008` | `8009` |

### 鍵の生成

> 💡 **validator_start_indexについて：**
> 鍵の番号はVMごとではなく**ニーモニックグループ全体**で管理します。
>
> | index | 鍵 | 用途 |
> |---|---|---|
> | 0 | 鍵A | testnetcsm2（スラッシング済み） |
> | 1 | 鍵B | testnetcsm2（スラッシング済み） |
> | 2 | 鍵C | testnetcsm2（Depositable） |
> | **3** | **鍵D** | **testnetcsm（今回）** |
>
> index 0〜2はすでに使用済みのため
> testnetcsmでは `--validator_start_index 3` を指定します。

```bash
# csm-artifactsの確認
ls ~/csm-artifacts/ 2>/dev/null || echo "csm-artifactsが存在しません"

# 存在しない場合は以下でダウンロード
mkdir -p ~/csm-artifacts
cd ~/csm-artifacts
RELEASE_URL=$(curl -s https://api.github.com/repos/ethstaker/ethstaker-deposit-cli/releases/latest \
  | jq -r '.assets[] | select(.name | contains("linux-amd64") and (contains("sha256")|not)) | .browser_download_url')
wget $RELEASE_URL -O ethstaker_deposit-cli-linux-amd64.tar.gz
tar -xvf ethstaker_deposit-cli-linux-amd64.tar.gz
```

```bash
# 既存ファイルの確認（古いファイルが残っていないか）
ls ~/csm-artifacts/ethstaker_deposit-cli-*/validator_keys/ 2>/dev/null

# 鍵の生成（index 3から）
cd ~/csm-artifacts/ethstaker_deposit-cli-*-linux-amd64/
./deposit existing-mnemonic \
  --num_validators 1 \
  --validator_start_index 3 \
  --chain hoodi \
  --eth1_withdrawal_address 0x4473dCDDbf77679A643BdB654dbd86D67F8d32f2
```

実行すると以下の対話形式で進みます：

```
# 言語選択
Please choose your language [...]: [English] → Enterキー

# インターネット接続警告
*** Internet connectivity detected ***
...
Press any key to continue... → Enterキー

# start indexの確認入力
Please repeat the validator start index to confirm: → 3

# withdrawal addressの確認入力（注意！）
Repeat your withdrawal address for confirmation.:
→ 0x4473dCDDbf77679A643BdB654dbd86D67F8d32f2

# ニーモニックの入力
Please enter your mnemonic separated by spaces:
→ 24単語のニーモニックを入力

# Compounding validators（0x02）の確認
Please enter yes if you want to generate compounding validators...
[no]: → Enterキー（noのまま）

# パスワードの設定
Enter password for validator keystore(s):
→ 任意のパスワードを入力

Repeat for confirmation:
→ 同じパスワードを再入力
```

> ⚠️ **withdrawal addressの確認入力に注意してください。**
> `start index`の確認入力（数字）の直後に来るため
> withdrawal addressの全文（`0x4473...`）を正確に入力してください。
> 数字だけを入力するとエラーになります。

> 💡 **Compounding validatorsについて：**
> `no`（デフォルト）を選択します。
> Lido CSMは32 ETH標準バリデータ（0x01）を使用するため
> `no`が正解です。

**生成完了メッセージ：**
```
Creating your keys.
Creating your keys:               [####] 1/1
Creating your keystore-*.json file(s): [####] 1/1
Creating your deposit_data-*.json file(s): [####] 1/1
Verifying your keystore-*.json file(s): [####] 1/1
Verifying your deposit_data-*.json file(s): [####] 1/1
Success!
Your keys can be found at: /home/test/csm-artifacts/.../validator_keys
```

> ⚠️ **生成後に古いファイルが混在していないか確認してください。**
> 今回生成した鍵のファイル名には `_3_0_0_` が含まれています。
> ```
> keystore-m_12381_3600_3_0_0-XXXXXXXXXX.json  ← 正しい（index 3）
> ```

### 鍵のインポート手順

```bash
# /tmpに鍵ファイルをコピー
sudo mkdir -p /tmp/keys_import
sudo cp -r ~/csm-artifacts/ethstaker_deposit-cli-*/validator_keys /tmp/keys_import/
sudo chown -R consensus:consensus /tmp/keys_import

# Nimbusでインポート
sudo -u consensus /usr/local/bin/nimbus_beacon_node deposits import \
  --data-dir=/var/lib/nimbus \
  /tmp/keys_import/validator_keys

# /tmpの鍵を削除
sudo rm -rf /tmp/keys_import
```

**完了メッセージ：**
```
NTC Keystore imported
  file=.../keystore-m_12381_3600_3_0_0-XXXXXXXXXX.json
```

### Lido CSMへの登録

鍵のインポート完了後、deposit_dataをLido CSMウィジェットに登録します。

```bash
# deposit_dataの内容を確認
cat ~/csm-artifacts/ethstaker_deposit-cli-*/validator_keys/deposit_data-*.json
```

> 💡 **Lido CSMへの登録手順は第2部と同じです。**
> `deposit_data-XXXXXXXXXX.json` の内容を
> `csm.testnet.fi` のウィジェットに貼り付けてください。
>
> ⚠️ **貼り付け時は `[ ]`（角括弧）ごとコピーしてください。**
> ファイルの内容全体（`[{...}]`の形式）をそのまま貼り付けます。
> 角括弧を省略するとエラーになります。
>
> 登録後にCSM Sentinelから以下の通知が届きます：
> ```
> 👀 New keys uploaded
>   Keys count: X -> X+1
> ```

### systemdサービスの設定

```bash
sudo vi /etc/systemd/system/nimbus-vc.service
```

```ini
[Unit]
Description=Nimbus Validator Client service for Hoodi
Wants=network-online.target
After=network-online.target nimbus.service
Documentation=https://www.coincashew.com

[Service]
Type=simple
User=consensus
Group=consensus
Restart=on-failure
RestartSec=30
KillSignal=SIGINT
TimeoutStopSec=900
ExecStart=/usr/local/bin/nimbus_validator_client \
  --data-dir=/var/lib/nimbus \
  --beacon-node=http://127.0.0.1:5052 \
  --metrics \
  --metrics-port=8009 \
  --suggested-fee-recipient=<Lido_CSM_Fee_Recipient_Address>

[Install]
WantedBy=multi-user.target
```

> 💡 **`RestartSec=30` について：**
> 起動失敗時の再起動間隔を30秒にします。
> 3秒のままにするとエラー時に大量のプロセスが
> 生成されVMがフリーズする原因になります。

> ⚠️ **`--keymanager`オプションについて：**
> `--keymanager`を使う場合は
> `--keymanager-token-file`の指定が必須です。
> 今回はKeymanager APIは不要なため省略しています。

> 💡 **末尾の`\`に注意：**
> 最終行のオプションに`\`を付けると起動エラーになります。
> 最終行には`\`を付けないでください。

### 起動と確認

> ⚠️ **Nimbus BNを先に起動してからVCを起動してください。**
> 順序を守らないとVCが先に起動してエラーになります。

```bash
# ① BNが起動していることを確認
sudo systemctl is-active nimbus

# ② BNがactiveの場合のみVCを起動
sudo systemctl daemon-reload
sudo systemctl enable nimbus-vc
sudo systemctl start nimbus-vc
sudo systemctl is-active nimbus-vc

# ③ ログ確認
sudo journalctl -fu nimbus-vc
```

**正常起動時のログ：**
```
NTC Local validator attached   pubkey=839cbc13...
NTC Beacon node is online
NTC Beacon node is in sync     sync_distance=0
INF Slot start  validators=1   node_status=synced
```

> 💡 **`attestationIn=<unknown>`について：**
> Lido CSMへの登録後・デポジット完了前は
> この表示が続きます。Activeになると実際の値に変わります。

---

### 鍵登録後の待機：Depositable → Active

Lido CSMウィジェットへの登録完了後、すぐにアテステーションが始まるわけではありません。
以下の流れでステータスが変化します。

```
deposit_dataを登録

↓

Depositable（Lidoのデポジット処理待ち）

↓ 数時間〜数日（テストネットの混雑状況による）

Active（アテステーション開始）✅
```

**CSM Sentinelからの通知で確認できます：**

```
👀 New keys uploaded       ← 登録完了

🤩 Keys were deposited     ← デポジット完了・間もなくActive
```

**Lido CSMウィジェット（csm.testnet.fi）の Keys タブで状態を確認できます：**

| ステータス | 意味 |
|---|---|
| `Depositable` | Lidoのデポジット処理待ち。しばらく待つ |
| `Active` | アテステーション開始 ✅ |

**VCログでActiveを確認：**

```bash
sudo journalctl -u nimbus-vc -f -o cat | grep -i "attestation\|active\|inactive"
```

| ログ | 意味 |
|---|---|
| `attestationIn=<unknown>` | Depositable状態。まだ署名できない |
| `Attestation published` | Active確定・署名開始 ✅ |

> 💡 **今回の実測（Hoodi Testnet / 2026年6月）：**
> deposit_data登録から約2日後にActiveを確認しました。
> テストネットはメインネットよりキュー消化が遅い場合があります。

---

### トラブルシューティング：VMフリーズとkeystoreロックエラー

> 💡 **本章の手順通りに構築した場合、このトラブルは発生しません。**
> セクション5でBN用の空ディレクトリ（`/var/lib/nimbus-bn/`）を
> 作成している場合は、この手順は不要です。
> 以下は手順を省略した場合や
> VMを強制終了した後に発生した際の復旧手順です。

**症状：**
```
ERR Unable to lock keystore file
  error_msg="Resource temporarily unavailable"
FAT Unable to load keystore
```

**原因：**
```
【一次原因】設定の問題
Nimbus BNが --in-process-validators=false を指定しても
同じ --data-dir のvalidatorsディレクトリの
keystoreをロックしてしまう

→ nimbus-vcが起動できず3秒ごとに再起動（当初のRestartSec=3が原因）
→ 大量プロセスがVMのリソースを消費
→ VMがフリーズ

【二次原因】強制終了によるWALファイルの残存
VMのフリーズ後に強制終了したことで
nimbus-vcが正常にシャットダウンできず
SQLiteのWALファイルが残ったまま再起動した

  slashing_protection.sqlite3-shm
  slashing_protection.sqlite3-wal

→ nimbus-vcが「他のプロセスが使用中」と判断
→ 起動できない状態が続く

【対処の流れ】
  WALファイルの削除 → BN設定の修正 → 正常起動
```

**解決手順：**

```bash
# 1. 両サービスを停止
sudo systemctl stop nimbus-vc
sudo systemctl stop nimbus

# 2. WALファイルを削除（二次原因の解消）
# 強制終了後に残存したSQLiteのWALファイルを削除する
sudo ls /var/lib/nimbus/validators/
sudo rm -f /var/lib/nimbus/validators/slashing_protection.sqlite3-shm
sudo rm -f /var/lib/nimbus/validators/slashing_protection.sqlite3-wal

# 削除確認（.sqlite3ファイルのみ残っていればOK）
sudo ls /var/lib/nimbus/validators/
```

> 💡 **WALファイルとは：**
> SQLiteデータベースの書き込みログファイルです。
> 正常にシャットダウンされた場合は自動的に削除されますが
> 強制終了した場合は残ったままになります。
> このファイルが残っていると次回起動時に
> 「Resource temporarily unavailable」エラーが発生します。

```bash
# 3. BN用の空ディレクトリを作成（一次原因の解消）
sudo mkdir -p /var/lib/nimbus-bn/validators
sudo mkdir -p /var/lib/nimbus-bn/secrets
sudo chown -R consensus:consensus /var/lib/nimbus-bn

# 4. nimbus.serviceに以下のオプションを追加
sudo vi /etc/systemd/system/nimbus.service
```

追加するオプション（`--data-dir`の直後）：
```ini
  --validators-dir=/var/lib/nimbus-bn/validators \
  --secrets-dir=/var/lib/nimbus-bn/secrets \
```

```bash
# 5. nimbus-vc.serviceのRestartSecを変更
sudo vi /etc/systemd/system/nimbus-vc.service
# RestartSec=3 → RestartSec=30 に変更

# 6. 設定を反映してBNを先に起動
sudo systemctl daemon-reload
sudo systemctl start nimbus
sleep 20

# 7. BNがactiveになってからVCを起動
sudo systemctl is-active nimbus
sudo systemctl start nimbus-vc
sudo systemctl is-active nimbus nimbus-vc
```

> ⚠️ **BNを先に起動してから20秒待ってVCを起動してください。**
> 起動順序を守らないとVCが先に起動してエラーになります。

**確認できた正常なディレクトリ構成：**

```
/var/lib/nimbus/          ← VCが使うメインディレクトリ
  db/                     ← BNのチェーンデータ
  secrets/                ← VCのパスワードファイル
  validators/             ← VCの鍵データ・slashing_protection DB

/var/lib/nimbus-bn/       ← BN用の空ディレクトリ
  secrets/                ← 空
  validators/             ← 空（BNはここを参照する）
```

---

## 7. 補足：EthPillar の紹介

手動構築の手順を理解した上で、より効率的な方法も紹介します。

**EthPillar**は CoinCashew が提供するフレンドリーなノードインストーラーです。

````
特徴：
  ✅ Nimbus + Nethermind（マイノリティ構成）を数分でデプロイ
  ✅ MEV-Boost 対応
  ✅ Lido CSM 対応
  ✅ TUI（テキストUI）で操作が簡単
  ✅ アップデートも簡単
````

📎 **公式サイト：** [https://docs.coincashew.com/guides/testnet-hoodi](https://docs.coincashew.com/guides/testnet-hoodi)

> 💡 **本章では手動構築を選択しました。**
> 「なぜこのコマンドを打つのか」を理解することが重要だからです。
> EthPillarは手順を理解した上で使うとより効果的です。

---

## 8. まとめ

---

> 💡 **【VM環境での比較】Lighthouse vs Nimbus：どちらが安定するか**
>
> 稼働2日後の実測で、VM（NAT）環境ではNimbusのピア数が7〜9に留まり
> `Peer count low, no new peers discovered` の警告が継続しました。
> これはVirtualBox NATのインバウンド制限によるもので、UFWの設定は正常です。
>
> | 項目 | Lighthouse（第1部・VM） | Nimbus（第3章・VM） |
> |---|---|---|
> | ピア数 | 10〜30 | **7〜9**（頭打ち） |
> | アテステーション遅延 | 軽微 | **delay 629ms〜1.4s** |
> | BNタイムアウト | なし | **断続的に発生** |
> | 署名継続性 | 安定 | **継続するが遅延あり** |
>
> **結論：VM環境ではLighthouseの方が安定する。**
> Nimbusの真価（省リソース・高速Checkpoint Sync）は
> ベアメタル環境で発揮される。
> ネットワーク多様化への貢献という観点では引き続きNimbusを推奨するが
> VM検証環境としてはLighthouseが適している。

---

| 知見 | 内容 |
|---|---|
| クライアント多様性 | Nethermind（38%）+ Nimbus（9.7%）を選ぶことでネットワークの健全化に貢献 |
| Nethermind同期速度 | 約3時間15分（Gethの約半分）。4段階に分かれた効率的な同期 |
| Nethermind同期4段階 | Old Headers→Snap State Phase 1→Large storage→Old Bodies |
| Nimbus Checkpoint Sync | 約2分15秒で署名開始可能。backfill（8時間32分）はバックグラウンド処理 |
| Doppelgänger Detection | Nimbusはデフォルトで有効。第2章のtestnetcsmは無効だった |
| リソース効率 | 安定後Swap 449MB・ディスク107GB（Geth+Lighthouseより41GB少ない） |
| ホストPCメモリ | Geth+Lighthouseの約1/3（4.3GB）。8GB RAMでも運用可能 |
| fee-recipient設定 | NimbusはBN側で設定する（Lighthouseとの重要な違い） |
| BN/VC分離の注意点 | Nimbusはデフォルトでin-process validator推奨。分離は上級者向け |
| keystoreロック対策 | --validators-dirと--secrets-dirでBN用の空ディレクトリを指定する |
| RestartSec設定 | nimbus-vcはRestartSec=30推奨。3秒のままにするとVMフリーズの原因になる |
| Nimbusの同期戦略 | オプティミスティック設計。Checkpoint Sync（約2分15秒）で即座に署名開始。backfill（約8時間32分）は収益に影響しないバックグラウンド処理 |
| アテステーション効率 | 稼働後に追記予定。現時点では両クライアント間の差異は未確認 |
| 稼働2日後の安定期リソース（VM・1バリデータ） | メモリ5.9GB / Swap510MB / ディスク111GB / Nimbus BN CPU46.5% / Nethermind CPU46.3% |
| ピア数の実測（VM NAT環境） | 7〜9ピア（wanted:100に対して）。discovered_nodes=0が継続。UFWは正常だがVirtualBox NATのインバウンド制限が原因 |
| VCタイムアウトとの関係 | ピア不足によりBNの応答が遅延→VCがタイムアウト→アテステーション遅延（delay 1s387ms〜629ms）。署名自体は継続 |
| Lighthouse（第1部）との比較 | VM環境ではピア数・アテステーション安定性ともにLighthouseが優位。Nimbusの真価はベアメタル環境で発揮される |

---

### 【総括】VM環境でのクライアント選択指針

> 参考実測値（Hoodi Testnet / VM環境 / 稼働2日後）

**安定稼働後リソース比較：**

| 項目 | Geth+Lighthouse（第1部） | Nethermind+Nimbus（本章） |
|---|---|---|
| ホストPCメモリ割り当て | 14,168MB（13.8GB） 🔴 | **4,384MB（4.3GB）** ✅ |
| VM内メモリ使用（安定後） | - | **5.9GB** |
| Swap（安定後） | - | **510MB** |
| ディスク使用（安定後） | 148GB | **111GB** ✅ |
| Nethermind/Geth CPU（安定後） | - | **46.3%** |
| Nimbus/Lighthouse BN CPU（安定後） | - | **46.5%** |
| ホストPCディスクI/O | 93.5 MB/s 🔴 | **2.1 MB/s** ✅ |

**ピア数・アテステーション安定性（VM NAT環境）：**

| 項目 | Lighthouse（第1部・VM） | Nimbus（本章・VM） |
|---|---|---|
| ピア数 | 10〜30 | **7〜9**（頭打ち） |
| アテステーション遅延 | 軽微 | **47ms〜1.4s** |
| BNタイムアウト | なし | **断続的に発生** |
| 原因 | - | VM NAT インバウンド制限 |

**VM環境でのクライアント選択まとめ：**

リソース効率を優先するなら：

→ Nethermind + Nimbus

→ ホストPCメモリが1/3・ディスクI/Oが激減

→ 8GB RAMのPCでも運用可能

アテステーション安定性を優先するなら：

→ Geth + Lighthouse

→ ピア数が安定・BNタイムアウトなし

→ VM NAT環境でも安定稼働

ネットワーク多様化への貢献：

→ Nethermind（38%）+ Nimbus（9.7%）を選ぶことが推奨

→ ただしVM環境では性能に制約あり

→ Nimbusの真価はベアメタル環境で発揮される

結論（VM検証環境）：

学習・検証目的 → どちらでも可

ネットワーク貢献重視 → Nethermind + Nimbus

安定性重視 → Geth + Lighthouse

---

> 💡 **第4部・第4章へ続く（執筆予定）**
> 第4章：Voluntary Exit（バリデータの正規引退）
> 新しい鍵でしばらく運用後にVoluntary Exitを実施し
> 「正しい引退の手順」を記録します。
