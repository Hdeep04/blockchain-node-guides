# 第4部・応用編 第3章：マイノリティクライアントへの移行
## ～ Nethermind + Nimbus 構築記録 ～

> **第1部・第2部で構築したGeth + Lighthouseから、マイノリティクライアントであるNethermind + Nimbusへの移行を検証した記録**

---

| 項目 | 内容 |
|---|---|
| 位置づけ | 第4部・応用編の第3章。マイノリティクライアントの構築と第1部・第2部との比較 |
| 検証環境 | VirtualBox VM（Ubuntu 22.04 LTS）× 1台（testnetcsm） |
| 前提 | 第1部・第2部でGeth + Lighthouseの構築が完了していること |
| 検証日 | 2026年6月13日（Hoodi Testnet） |

> ⚠️ **本書は機密情報を一切含みません。** アドレス類は `<...>` のプレースホルダです。

> 💡 **リンクは右クリック→「新しいタブで開く」を選択すると
> 手順書を表示したまま参照先を確認できます。**

---

## 1. マイノリティクライアントとは

Ethereumネットワークには複数の実装（クライアント）が存在します。ノードオペレーターがどのクライアントを使うかは自由ですが、特定のクライアントに集中しすぎると**ネットワーク全体のリスクが高まります。**

### クライアント多様性の重要性

````
【危険なシナリオ】

あるクライアントが全ノードの66%以上を占めている場合：
  そのクライアントに重大なバグが発生
      ↓
  66%以上のノードが誤った判断をする
      ↓
  チェーンが不正なまま確定（ファイナライズ）
      ↓
  大規模なスラッシング発生
  ネットワーク停止の可能性
````

> 💡 **33%ルール：**
> Ethereum.orgは「どのクライアントも33%を超えるべきではない」と推奨しています。
> 33%を超えると単一障害点になりうるからです。

### 2026年現在のクライアントシェア

**実行レイヤー（EL）：**

| クライアント | シェア | 評価 |
|---|---|---|
| **Geth** | **41%** | 多すぎる・削減すべき |
| **Nethermind** | **38%** | 適切 |
| Besu | 16% | 少ない |
| Reth | 5.4% | 少ない |

**合意レイヤー（CL）：**

| クライアント | シェア | 評価 |
|---|---|---|
| **Lighthouse** | **52.65%** | 多すぎる・削減すべき |
| Prysm | 17.66% | やや多い |
| Teku | 13.36% | 適切 |
| **Nimbus** | **9.7%** | 少ない・使うべき |

### なぜNethermind + Nimbusを選ぶのか

````
Geth（41%）の代わりにNethermindを使う
  → 実行レイヤーの多様化に貢献

Lighthouse（52%）の代わりにNimbusを使う
  → 合意レイヤーの多様化に貢献

さらに：
  Nimbusは「軽量・省リソース」が特徴
  → ホームステーキングに最適
  → 低スペックPCでも運用可能
````

---

## 2. 第1部・第2部との比較

### サービス・ユーザー・データ場所の対比

| 項目 | Geth（第1部・第2部） | Nethermind（本章） |
|---|---|---|
| サービス名 | `geth` | `nethermind` |
| 実行ユーザー | `ethereum` | `nethermind` |
| データ場所 | `/var/lib/ethereum/geth` | `/var/lib/nethermind` |
| JWT場所 | `/var/lib/ethereum/jwt/jwt.hex` | `/secrets/jwtsecret` |
| RPC Port | `8545` | `8545` |
| Engine Port | `8551` | `8551` |
| P2P Port | `30303` | `30303` |
| ネットワーク指定 | `--hoodi` | `--config hoodi` |
| Pruning | なし | `Hybrid`（標準搭載） |

| 項目 | Lighthouse（第1部・第2部） | Nimbus（本章） |
|---|---|---|
| サービス名 | `lighthouse` | `nimbus` |
| 実行ユーザー | `ethereum` | `consensus` |
| データ場所 | `/var/lib/ethereum/lighthouse` | `/var/lib/nimbus` |
| JWT場所 | `/var/lib/ethereum/jwt/jwt.hex` | `/secrets/jwtsecret` |
| REST Port | `5052` | `5052` |
| Metrics Port | `5054` | `8008` |
| fee-recipient設定場所 | VC側 | **BN側** |
| Doppelgänger Detection | 無効（デフォルト） | **有効（デフォルト）** ✅ |

### コマンドの違い

**Geth vs Nethermind：ネットワーク指定**

```bash
# Geth：フラグで指定
ExecStart=/usr/bin/geth \
  --hoodi \          ← フラグ

# Nethermind：設定ファイル名で指定
ExecStart=/usr/local/bin/nethermind/nethermind \
  --config hoodi \   ← 設定ファイル名
```

**Lighthouse vs Nimbus：同期方法**

```bash
# Lighthouse：サービス起動時にCheckpoint Sync
ExecStart=/usr/local/bin/lighthouse beacon_node \
  --checkpoint-sync-url https://...   ← 起動オプション

# Nimbus：起動前に別コマンドで実行
sudo -u consensus /usr/local/bin/nimbus_beacon_node trustedNodeSync \
  --network=hoodi \
  --trusted-node-url=https://hoodi.beaconstate.ethstaker.cc \
  --data-dir=/var/lib/nimbus \
  --backfill=false
```

> 💡 **NimbusのCheckpoint Syncは驚異的な速さでした。**
> 今回の実測：**わずか約2分15秒で完了**
> （03:13:47 開始 → 03:16:02 完了）

---

## 3. 検証環境

### testnetcsmの状態（構築前）

````
サービス：geth / lighthouse / lighthouse-vc 全て inactive・disabled
削除したデータ：
  /var/lib/ethereum/geth/      ← Gethのチェーンデータ
  /var/lib/ethereum/lighthouse/ ← Lighthouseのビーコンデータ
  /var/lib/lido-csm/validators/（古い鍵） ← スラッシング済み鍵

残したデータ：
  /var/lib/lido-csm/           ← lighthouse-vc関連
  /etc/systemd/system/         ← 既存サービスファイル（後で置き換え）
````

### 新規作成したリソース

````
/secrets/jwtsecret             ← JWT Secret（新規生成）
/var/lib/nethermind/           ← Nethermindデータディレクトリ
/var/lib/nimbus/               ← Nimbusデータディレクトリ
````

---

## 4. Nethermind のインストール

### ユーザーとディレクトリの作成

```bash
# Nethermind専用ユーザーの作成
# なぜ専用ユーザーを作るのか：
# 万が一の侵害時にシステム全体への影響を最小化するため
sudo useradd --no-create-home --shell /bin/false nethermind
sudo mkdir -p /var/lib/nethermind
sudo chown -R nethermind:nethermind /var/lib/nethermind
```

### バイナリのダウンロードとインストール

```bash
# 最新バージョンを自動取得
RELEASE_URL="https://api.github.com/repos/NethermindEth/nethermind/releases/latest"
BINARIES_URL="$(curl -s $RELEASE_URL | jq -r '.assets[] | select(.name) | .browser_download_url' | grep linux-x64 | grep -v asc)"

echo "Downloading URL: $BINARIES_URL"

cd ~
wget -O nethermind.zip $BINARIES_URL
sudo mkdir -p /usr/local/bin/nethermind
sudo unzip nethermind.zip -d /usr/local/bin/nethermind
rm nethermind.zip

# バージョン確認
/usr/local/bin/nethermind/nethermind --version
```

**今回の実測：**
````
Version: 1.38.0+c07a4d65
Runtime: .NET 10.0.7
Platform: Linux x64
````

### JWT Secretの作成

```bash
# /secrets フォルダを作成
# なぜ/secrets か：
# システム全体で使う認証情報を一か所にまとめるため
sudo mkdir -p /secrets
openssl rand -hex 32 | tr -d "\n" | sudo tee /secrets/jwtsecret
sudo chmod 644 /secrets/jwtsecret
```

### systemdサービスの設定

```bash
sudo vi /etc/systemd/system/nethermind.service
```

```ini
[Unit]
Description=Nethermind Execution Layer Client service for Hoodi
Wants=network-online.target
After=network-online.target
Documentation=https://www.coincashew.com

[Service]
Type=simple
User=nethermind
Group=nethermind
Restart=always
RestartSec=3
KillSignal=SIGINT
TimeoutStopSec=900
WorkingDirectory=/var/lib/nethermind
Environment="DOTNET_BUNDLE_EXTRACT_BASE_DIR=/var/lib/nethermind"
ExecStart=/usr/local/bin/nethermind/nethermind \
  --config hoodi \
  --datadir="/var/lib/nethermind" \
  --Network.DiscoveryPort 30303 \
  --Network.P2PPort 30303 \
  --Network.MaxActivePeers 50 \
  --JsonRpc.Port 8545 \
  --JsonRpc.EnginePort 8551 \
  --Metrics.Enabled true \
  --Metrics.ExposePort 6060 \
  --JsonRpc.JwtSecretFile /secrets/jwtsecret

[Install]
WantedBy=multi-user.target
```

### 起動と確認

```bash
sudo systemctl daemon-reload
sudo systemctl enable nethermind
sudo systemctl start nethermind
sudo systemctl is-active nethermind
```

```bash
# ログをリアルタイムで確認する
# 「Initialization Completed」が表示されれば起動成功
sudo journalctl -fu nethermind
```

**起動直後の重要なログ：**

````
Initialization Completed
Chain ID: Hoodi
JSON RPC: http://127.0.0.1:8545 ; http://127.0.0.1:8551
Reading authentication secret from '/secrets/jwtsecret'
Waiting for Forkchoice message from Consensus Layer
````

> 💡 **「Waiting for Forkchoice message from Consensus Layer」について：**
> Post-Merge以降のEthereumでは、EL（Nethermind）はCLからの
> Forkchoiceメッセージが届くまで同期を開始できません。
> これはGethも同様で、CLクライアント（Nimbus）を
> 起動するまでこの状態が続きます。これは正常な動作です。

---

## 5. Nimbus のインストール

### ユーザーとディレクトリの作成

```bash
# consensus専用ユーザーの作成
sudo adduser --system --no-create-home --group consensus
sudo mkdir -p /var/lib/nimbus
sudo chown -R consensus:consensus /var/lib/nimbus

# 依存パッケージのインストール
sudo apt install libsnappy-dev libc6-dev libc6 ccze -y
```

### バイナリのダウンロードとインストール

```bash
RELEASE_URL="https://api.github.com/repos/status-im/nimbus-eth2/releases/latest"
BINARIES_URL="$(curl -s $RELEASE_URL | jq -r '.assets[] | select(.name) | .browser_download_url' | grep _Linux_amd64 | grep -v asc)"

echo "Downloading URL: $BINARIES_URL"

cd ~
wget -O nimbus.tar.gz $BINARIES_URL
tar -xzvf nimbus.tar.gz -C ~
mv ~/nimbus-eth2_Linux_amd64_* ~/nimbus
sudo mv ~/nimbus/build/nimbus_beacon_node /usr/local/bin
sudo mv ~/nimbus/build/nimbus_validator_client /usr/local/bin
rm nimbus.tar.gz
rm -rf ~/nimbus

# バージョン確認
nimbus_beacon_node --version
```

**今回の実測：**
````
Nimbus beacon node v26.5.0-6fb05f-stateofus
Ethereum consensus spec v1.7.0-alpha.7
````

### Checkpoint Syncで高速同期

```bash
# 起動前に信頼できるノードから最新状態を取得する
# なぜCheckpoint Syncを使うのか：
# 通常の同期は数時間〜数日かかるが
# Checkpoint Syncを使えば数分で完了する
sudo -u consensus /usr/local/bin/nimbus_beacon_node trustedNodeSync \
  --network=hoodi \
  --trusted-node-url=https://hoodi.beaconstate.ethstaker.cc \
  --data-dir=/var/lib/nimbus \
  --backfill=false
```

**完了メッセージ：**
````
NTC Done, your beacon node is ready to serve you!
````

> 💡 **Checkpoint Syncの実測時間：約2分15秒**
> 03:13:47 開始 → 03:16:02 完了
> Lighthouseの場合と同様に数分で完了します。

### systemdサービスの設定

```bash
sudo vi /etc/systemd/system/nimbus.service
```

```ini
[Unit]
Description=Nimbus Consensus Layer Client service for Hoodi
Wants=network-online.target
After=network-online.target
Documentation=https://www.coincashew.com

[Service]
Type=simple
User=consensus
Group=consensus
Restart=on-failure
RestartSec=3
KillSignal=SIGINT
TimeoutStopSec=900
ExecStart=/usr/local/bin/nimbus_beacon_node \
  --network=hoodi \
  --data-dir=/var/lib/nimbus \
  --tcp-port=9000 \
  --udp-port=9000 \
  --max-peers=100 \
  --rest-port=5052 \
  --enr-auto-update=true \
  --non-interactive \
  --status-bar=false \
  --in-process-validators=false \
  --web3-url=http://127.0.0.1:8551 \
  --rest \
  --metrics \
  --metrics-port=8008 \
  --jwt-secret="/secrets/jwtsecret" \
  --suggested-fee-recipient=<Lido_CSM_Fee_Recipient_Address>

[Install]
WantedBy=multi-user.target
```

> 💡 **`--suggested-fee-recipient` について：**
> Nimbusでは**BN側**でfee-recipientを設定します。
> Lighthouseでは**VC側**で設定していた点が異なります。
>
> | ネットワーク | Lido CSM Fee Recipient Address |
> |---|---|
> | Hoodi Testnet | `0x9b108015fe433F173696Af3Aa0CF7CDb3E104258` |
> | Mainnet | `0x388C818CA8B9251b393131C08a736A67ccB19297` |

> 💡 **`--in-process-validators=false` について：**
> BNとVCを分離して動かすための設定です。
> Nimbusは本来BNとVCを一体化して動かすこともできますが
> 第1部・第2部と同様にBN/VC分離構成を採用します。

> 💡 **`doppelgangerDetection: true`（デフォルト有効）**
> Nimbusは**デフォルトでDoppelgänger Detectionが有効**です。
> 第2章で検証したtestnetcsmでは無効になっていましたが
> Nimbusでは何もしなくても有効になっています。

### 起動と確認

```bash
sudo systemctl daemon-reload
sudo systemctl enable nimbus
sudo systemctl start nimbus
sudo systemctl is-active nimbus
```

```bash
# ログをリアルタイムで確認する
# 「Slot start」が表示されれば起動成功
sudo journalctl -fu nimbus
```

**正常起動時のログ例：**
````
NTC Starting REST HTTP server  url=http://127.0.0.1:5052
NTC Starting beacon node       validators=0
INF Slot start  slot=XXXXXX  epoch=XXXXXX  peers=X
````

**Nimbusが起動するとNethermindに変化が現れます：**
````
Received ForkChoice: ...
New beacon pivot: ...
Old Headers X / X (XX%)
Snap State Ranges (Phase 1): X%
````

---

## 6. リソース比較（実測値）

### VM内リソース比較

検証日：2026年6月13日
環境：Ubuntu 22.04 LTS / RAM 15GB / SSD 292GB

**同期中のリソース使用量：**

| 項目 | Geth+Lighthouse（testnetcsm2） | Nethermind+Nimbus（testnetcsm） |
|---|---|---|
| **メモリ使用** | 5.2GB | **5.1GB** |
| **Swap使用** | **1.2GB** 🔴 | **0B** ✅ |
| **ディスク使用** | **148GB**（稼働1日後） | **15GB**（同期中） |

**プロセス別リソース：**

| プロセス | CPU | メモリ | 備考 |
|---|---|---|---|
| Lighthouse BN | **221%** 🔴 | 22.5%（3.7GB） | 長期稼働・高負荷 |
| Geth | 32.9% | 9.5%（1.6GB） | 稼働中 |
| Lighthouse VC | 0.1% | 0.1% | 軽量 |
| **Nethermind** | **91.5%** | 14.5%（2.2GB） | 同期中 |
| **Nimbus BN** | **43.2%** | 17.7%（2.9GB） | 同期中 |

> 💡 **CPU使用率について：**
> 同期中はCPUが高くなります。同期完了後は大幅に下がります。
> Lighthouseの221%は長期稼働による蓄積の可能性があります。

### ホストPC（Windows 11）視点での比較

| VM | メモリ割り当て | CPU |
|---|---|---|
| testnetcsm2（Geth+Lighthouse） | **14,168 MB（13.8GB）** 🔴 | 19.7% |
| testnetcsm（Nethermind+Nimbus） | **4,384 MB（4.3GB）** ✅ | 2.7% |

````
Nethermind + Nimbusは
Geth + Lighthouseの約1/3のメモリで動作

→ 8GB RAMのPCでもホームステーキングが可能
→ 低スペック・省電力PCでの運用を実現
→ コスト削減とネットワーク貢献を同時に実現
````

---

## 7. Nimbus VC のインストール（続報）

> ⚠️ **本セクションは構築継続中です。完了後に追記します。**

### 記録予定の項目

- [ ] Nimbus VC のサービス設定
- [ ] 鍵のインポート手順
- [ ] lighthouse-vcとの設定比較
- [ ] `All validators active` の確認

---

## 8. 補足：EthPillar の紹介

手動構築の手順を理解した上で、より効率的な方法も紹介します。

**EthPillar**は CoinCashew が提供するフレンドリーなノードインストーラーです。

````
特徴：
  ✅ Nimbus + Nethermind（マイノリティ構成）を数分でデプロイ
  ✅ MEV-Boost 対応
  ✅ Lido CSM 対応
  ✅ TUI（テキストUI）で操作が簡単
  ✅ アップデートも簡単
````

📎 **公式サイト：** [https://docs.coincashew.com/guides/testnet-hoodi](https://docs.coincashew.com/guides/testnet-hoodi)

> 💡 **本章では手動構築を選択しました。**
> 「なぜこのコマンドを打つのか」を理解することが重要だからです。
> EthPillarは手順を理解した上で使うとより効果的です。

---

## 9. まとめ

| 知見 | 内容 |
|---|---|
| クライアント多様性 | Nethermind（38%）+ Nimbus（9.7%）を選ぶことでネットワークの健全化に貢献 |
| Checkpoint Sync | Nimbusは約2分15秒で同期完了。Lighthouseと同等の速さ |
| Doppelgänger Detection | Nimbusはデフォルトで有効。第2章のtestnetcsmは無効だった |
| リソース効率 | Swap使用0B・ホストPCメモリ4.3GBのみ使用 |
| fee-recipient設定 | NimbusはBN側で設定する（Lighthouseとの重要な違い） |
| ホームステーキング | 8GB RAMでも運用可能。低コスト・省電力を実現 |

---

> 💡 **第4部・第4章へ続く（執筆予定）**
